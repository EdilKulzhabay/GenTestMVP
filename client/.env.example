# Бэкенд API (порт должен совпадать с server: process.env.PORT || 5000).
# Если не задано в dev — используется прокси Vite на 5000 (см. vite.config.ts).
# Не указывайте другой порт (например 5111), если сервер не слушает его.
# VITE_API_URL=http://localhost:5000/api/v1
